package com.chunyan.devpulsestudio.data

/** A real project selected by the generated daily snapshot, with an explicit reason label. */
data class DailyProject(val article: Article, val reason: String)
