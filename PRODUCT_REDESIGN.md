# DevPulse Studio 产品重构方案

## 1. 产品判断

DevPulse Studio 的唯一定位是「AI 开源项目发现与智能解读平台」。GitHub 只提供可核验的仓库元数据，首页不以浏览仓库为目的，而是帮助用户完成“发现、理解、判断、收藏、回访”的决策闭环。

北极星体验：用户在卡片上 10 秒判断赛道和用途，在详情页 1 分钟理解价值、适用人群与风险，并把值得跟进的项目沉淀到本地学习库。

## 2. 现状审计

### 可保留基础

- Kotlin、Fragment、ViewModel、StateFlow、Room、Retrofit、Navigation 结构清晰，适合渐进改造。
- 三个一级导航和本地收藏已经形成最小骨架。
- Coil、SwipeRefreshLayout 和 Material 3 依赖可以继续使用。

### 阻断产品价值的问题

- 数据查询固定为 Android/Kotlin，产品与 AI 开源定位相反。
- `Article` 只有标题、描述、作者、语言、Star 等基础字段，无法承载赛道、榜单、活跃度、结构化解读、风险和缓存。
- 网络异常被静默替换为三个虚构式“精选条目”，用户无法区分真实数据、缓存和失败状态。
- 发现页没有搜索、筛选、分页、榜单规则、同步时间及限流反馈。
- 卡片没有 AI 摘要层级、赛道标签、Fork、License、更新时间及快捷动作。
- 详情页只有标题、描述和外链，无法完成“快速理解与判断价值”。
- 收藏页仅复用列表，没有搜索、分类、统计、备份或学习状态。
- 关于页缺少设置、本地数据管理、隐私与 AI 免责声明。
- DayNight 主题虽然作为父主题存在，但颜色写死且状态栏亮色固定，实际深色模式不可用。
- 没有榜单和解读缓存，离线只能访问收藏；异常、空状态与限流没有语义区分。

## 3. 信息架构与 V1 范围

一级导航保持三个：发现、收藏、关于。详情是发现/收藏的二级页，底部导航在详情页隐藏。

### 发现

顶部品牌与刷新 → 全局搜索 → 赛道筛选 → 榜单筛选 → 规则与同步状态 → 项目决策卡片。赛道与榜单组合过滤，搜索覆盖仓库名、作者、描述、语言及赛道，500ms 防抖；单页 20 条，下拉刷新和触底分页。

### 详情

风险提示 → AI 极速解读 → 可信度说明 → 仓库事实 → 活跃度 → 深度 README 解读 → 相似项目。长内容折叠。没有云端 AI 服务时，只输出基于真实描述、topics、license、更新时间等可追溯信息的本地结构化速读，不生成无依据结论。

### 收藏

V1 首发实现本地收藏搜索、赛道分类、数量/偏好统计、统一项目卡片和 JSON 备份导出/导入。浏览历史、更新提醒和失效归档作为 V1.1 数据入口预留，但不展示无法工作的假 Tab。

### 关于

产品定位、主题设置、缓存策略、本地数据管理、版本与更新日志、数据源声明、隐私政策、AI 免责声明、风险项目警示、开源许可和联系渠道分区呈现。

## 4. 功能取舍说明

| 功能 | 为什么需要 | 解决问题 | 对体验/留存的帮助 | 版本 |
|---|---|---|---|---|
| 结构化速读 | 产品核心差异 | 英文 README 与仓库信息理解成本高 | 缩短首次价值时间 | V1 |
| 赛道 + 榜单筛选 | 提升发现效率 | 热门项目混杂、决策成本高 | 提升找到相关项目的成功率 | V1 |
| 本地缓存与明确错误态 | 保证可信可用 | 弱网、限流时白屏或假数据 | 降低失败流失 | V1 |
| 本地收藏、分类与备份 | 形成学习资产 | 收藏不可管理、重装易丢失 | 建立回访理由 | V1 |
| 风险提示与 AI 免责声明 | 合规底线 | 换脸等项目可能被滥用 | 建立信任 | V1 |
| 每日精选、趋势、更新推送 | 需要定时后端和历史快照 | 客户端无法可信计算全网趋势 | 稳定后驱动日活 | V1.1 |
| 笔记、学习状态、专题 | 学习管理深化 | V1 数据闭环尚未验证 | 增强长期资产 | V1.2 |
| 社区、评论、聊天 | 不服务核心闭环且成本高 | 会稀释定位 | 无 V1 价值 | 不做 |

## 5. 技术架构

- UI：Fragment + XML + Material 3，统一 8/16/24dp 间距、16dp 卡片圆角和语义颜色；WindowInsets 处理系统栏。
- 状态：一个页面一个不可变 `UiState`，显式区分首次加载、刷新、分页、缓存、空结果、网络、限流。
- 数据：Repository 作为单一入口；GitHub API DTO 与领域模型分离；Room 保存榜单快照、解读缓存和收藏。
- 缓存：榜单有效期 12 小时，项目解读 7 天；联网失败优先返回过期缓存并明确标注；30 天清理过期非收藏数据。
- AI：V1 本地速读器只根据可核验字段生成结构，输出“信息依据不足”而非绝对推荐。未来云端解读通过接口替换，领域模型不变。
- 安全：只申请网络权限；收藏、筛选历史与设置不上传；外链使用系统 Intent；导出由系统文件选择器授权。

## 6. 视觉与交互规则

- 首屏优先显示搜索、赛道、榜单和摘要，不展示代码目录等 GitHub 浏览器信息。
- 项目卡片依次为身份、真实指标、赛道标签、AI 速读、快捷动作；同级操作最多四个。
- 浅色/深色均使用主题属性，不在布局中写死背景和文字色。
- 动画仅使用刷新、收藏状态和页面淡入；遵循系统“减少动态效果”。
- 字体放大时允许内容换行，不使用固定高度；触控目标不小于 48dp。
- 无结果、无收藏、网络失败、限流和仅缓存状态使用不同文案与行动按钮。

## 7. V1 验收清单

- [ ] 首页默认只查询真实 AI 开源仓库，卡片无需进入详情即可判断赛道与用途。
- [ ] 搜索 500ms 防抖，赛道与五个榜单可组合，单页最多 20 条并支持继续加载。
- [ ] 网络失败和 GitHub 限流不伪造数据；有缓存显示缓存与同步时间，无缓存显示可重试错误。
- [ ] 详情首屏为结构化速读，包含概述、核心能力、适用人群、优势、短板、推荐指数与依据边界。
- [ ] 风险仓库每次首次进入详情前显示明确警示。
- [ ] 收藏完全本地化，可搜索、分类并导出备份。
- [ ] 仓库跳转、复制和分享均可用。
- [ ] 浅色/深色主题、系统栏和字体缩放正常。
- [ ] 关于页包含版本、更新日志、隐私、数据源、AI 与风险免责声明、许可及联系方式。
- [ ] Debug 构建、单元测试和关键状态人工检查通过。

## 8. 当前实现决策

- V1 使用 GitHub README 接口和本地证据化速读器。它只整理 README 标题、公开描述、Topics、许可证与指标，并清楚标注来源；没有可信模型服务时，不会输出伪造的“AI 深度结论”。
- 榜单的“24h / 7 日 / 30 日新晋”限定为对应时间窗内新建仓库再按 Stars 排序，不将其误称为 Star 增长榜。真正的增长、Release 和推送提醒需要后端定时快照，因此安排在 V1.1。
- 收藏升级为学习库，保存分类、收藏时间和学习状态；数据库通过迁移升级，不再用 destructive migration 删除用户资产。
- 相似项目推荐在 V1 先使用本地、可解释的赛道/语言/Topics 匹配；“不感兴趣”仅本地保存。云端个性化推荐应在具备用户授权、服务端特征与可解释策略后再接入。
- 搜索历史限制为最近 8 条且只保存在本机；它是降低重复检索成本的快捷入口，不用于画像上传或通知营销。
- 云端解读通过可配置的服务端网关接入；网关未配置或请求失败时自动回退本地速读，客户端不保存模型密钥。详细约束见 `AI_GATEWAY_CONTRACT.md`。
- V1.1 默认采用 GitHub Actions 生成静态目录作为每日发现与快照来源；客户端优先读取该目录，目录不可用时回退 GitHub 直连。该方案降低限流与重复计算成本，详见 `STATIC_CATALOG_PIPELINE.md`。
