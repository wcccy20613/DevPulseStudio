# DevPulse public catalog

`catalog.json` and `daily.json` are generated by `.github/workflows/generate-static-catalog.yml`.

For GitHub Pages, publish this repository from its default branch and configure the Android build property `STATIC_CATALOG_BASE_URL` to the URL ending in `/static/`. The workflow may also write this directory to an object store in a later deployment phase.
